services:
  env:
    image: docker.io/middle-management/ace-example:latest
    build: .
    restart: no
    secrets:
      - env
      - identity

secrets:
  env:
    file: ./.env.ace
  identity:
    file: ./identity
